public class ad7 {
  public static java.lang.String h1() {
    java.lang.String release = Build.VERSION.RELEASE;
    int sdkVersion = Build.VERSION.SDK_INT;
    return ((("Android SDK: " + sdkVersion) + " (") + release) + ")";
  }
}
